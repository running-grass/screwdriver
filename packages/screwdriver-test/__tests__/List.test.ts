import { ArrayList, map } from "screwdriver-js";

test("test map for List22", () => {
    let list = ArrayList.fromArray([1, 2]);
    // expect(map(a => a as number + 1, list)).toEqual(ArrayList.fromArray([2, 3]));
});
